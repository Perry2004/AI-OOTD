server:
  port: 3000

spring:
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
  devtools:
    livereload:
      enabled: true
    restart:
      enabled: true
  data:
    mongodb:
      uri: mongodb://mongodb:27017/ai-ootd